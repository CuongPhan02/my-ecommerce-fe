'use client'

export const useInitAuth = () => {
  // Since we are now using localStorage to store tokens, 
  // they persist across page reloads and are automatically 
  // picked up by the https interceptor.
  // No explicit initialization is required here.
}

